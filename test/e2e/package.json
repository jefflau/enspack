{
  "name": "@enspack/e2e",
  "version": "0.1.0",
  "private": true,
  "description": "enspack end-to-end suites: local Anvil swarm (CI) and Sepolia (nightly)",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "check": "pnpm typecheck && pnpm test"
  },
  "dependencies": {
    "@enspack/cli": "workspace:*",
    "@enspack/core": "workspace:*",
    "@enspack/torrent": "workspace:*",
    "viem": "^2.28.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
